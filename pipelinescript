pipeline {
    agent any 

    environment {
        AWS_ACCESS_KEY_ID     = credentials('AKIAX5UPQKDYTG7J5J5N')
        AWS_SECRET_ACCESS_KEY = credentials('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')
    }
    
    stages {
        stage('checkout') {
            steps {
                git 'https://github.com/Anbazhagansekar-cloud/terraform-3-tier-architecture.git'
            }
        }
        stage('init') {
            steps {
                sh 'terraform init'
            }
        }
        stage('validate') {
            steps {
                sh 'terraform validate'                
            }
        }
        stage('plan') {
            steps {
                sh 'terraform plan'
            }
        }
        stage('action') {
            steps {
                sh 'terraform $action --auto-approve'
            }
        }
    }
}
